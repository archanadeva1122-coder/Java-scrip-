# Java-scrip-
HTML is used to create input fields. CSS is used for styling and layout. JavaScript is used to validate the form by checking empty fields, email format, and phone number length. If the data is correct, the form is submitted successfully.
